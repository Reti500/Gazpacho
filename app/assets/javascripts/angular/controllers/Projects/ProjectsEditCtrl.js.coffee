@app.controller 'ProjectsEditCtrl', ['$scope', ($scope) ->
		# if $routeparams.id
		# 	$scope.project = Project.show({ id: $routeparams.id })
		# else
		# 	$scope.project = new Project()


		# $scope.submit = () ->
		# 	console.log("submit")

		# 	if($routeparams.id)
		# 		Project.update($scope.project, ($data) ->
		# 			console.log($data)
		# 		)
		# 	else
		# 		Project.create($scope.project, ($data) ->
		# 			console.log($data)
		# 		)
	]